{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd ../.. && (git lfs pull 2>/dev/null || true) && pnpm install",
  "framework": "nextjs",
  "crons": [{ "path": "/api/cron/rt", "schedule": "*/2 * * * *" }]
}
