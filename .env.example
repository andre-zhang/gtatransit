# Local development (PostGIS + full GTFS import)
DATABASE_URL=postgresql://gta:gta@localhost:5433/gta_transit
METROLINX_API_KEY=
YRT_GTFS_ZIP=
GTFS_DATA_DIR=./data/gtfs

# Demo mode: uses prebuilt fixtures in apps/web/demo (no database).
# Auto-enabled on Vercel when DATABASE_URL is unset.
# DEMO_MODE=1
